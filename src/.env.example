# Copy this file to .env and fill in values before running.

# API key(s) for machine/operator authentication.
# Comma-separated list of valid keys for round-robin or multi-tenant setups.
API_KEYS=changeme-operator-key-1,changeme-operator-key-2

# Port the HTTP server listens on.
PORT=3000

# SQLite database file path (relative to src/).
# Use an absolute path or a volume mount in production.
DB_PATH=./data/tito.db

# Ticket expiry in seconds (0 = never expires).
TICKET_TTL_SECONDS=86400

# Maximum value of a single ticket in cents.
MAX_TICKET_VALUE_CENTS=10000000

# Comma-separated list of valid property IDs.
# Leave blank to allow any property ID.
ALLOWED_PROPERTY_IDS=

# Log level: error | warn | info | debug
LOG_LEVEL=info
